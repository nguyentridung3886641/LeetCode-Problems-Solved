name: Update LeetCode Stats

on:
  schedule:
    - cron: '0 0 * * *'
  workflow_dispatch:

jobs:
  update-readme:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Repository
        uses: actions/checkout@v3

      - name: Update LeetCode Stats Badge
        uses: denalhb/leetcode-badge-action@v1.2
        with:
          username: nguyentridung3886641
          filename: README.md
